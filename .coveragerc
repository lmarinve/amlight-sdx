[run]
omit = venv/*, *tests*, *__init__*
[report]
exclude_lines =
    # Have to re-enable the standard pragma
    pragma: no cover
        if __name__ == .__main__.:
    # Don't complain if tests don't hit defensive assertion code:
    raise NotImplementedError